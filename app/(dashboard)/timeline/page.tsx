import TimelinePage from "@/components/timeline/timeline-page";

export const metadata = {
  title: "Timeline | Theta",
  description: "Visual planning roadmap — see when work is happening.",
};

export default function Page() {
  return <TimelinePage />;
}
