import InboxPage from "@/components/inbox/inbox-page";

export const metadata = {
  title: "Inbox | Theta",
  description: "Your workspace activity center — notifications, mentions, assignments, and direct messages.",
};

export default function Page() {
  return <InboxPage />;
}
