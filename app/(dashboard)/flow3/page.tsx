import { Flow3ChatPanel } from "@/components/flow3/chat-panel";

export const metadata = {
  title: "Flow³ AI | Theta PM",
  description: "Your AI copilot for projects, tasks, and workspace insights.",
};

export default function Page() {
  return <Flow3ChatPanel />;
}